[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ontos"
version = "0.1.0"
description = "本体智能体平台统一引擎库（Function/Action/Skill 注册表）"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["ontos*"]
